# URL of the deployed Vercel feedback function
VITE_FEEDBACK_URL=https://your-project.vercel.app/api/feedback

# --- Vercel env vars (set in Vercel dashboard, never committed) ---
# GITHUB_TOKEN=github_pat_...   (fine-grained token, Issues: Read & Write)
# GITHUB_REPO=owner/repo
